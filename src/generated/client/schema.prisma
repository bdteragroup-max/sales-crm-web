generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/client"
}

datasource db {
  provider = "postgresql"
}

model User {
  id                     String                @id @default(cuid())
  employeeId             String                @unique
  email                  String?               @unique
  fullName               String
  phoneNumber            String?
  role                   String                @default("Sales Representative")
  position               String?
  password               String
  createdAt              DateTime              @default(now()) @db.Timestamptz(6)
  updatedAt              DateTime              @default(now()) @updatedAt @db.Timestamptz(6)
  otpCode                String?
  otpExpiresAt           DateTime?             @db.Timestamptz(6)
  isActive               Boolean               @default(true)
  assignedCompanies      Company[]             @relation("AssignedCompany")
  companyInteractions    CompanyInteraction[]
  estimatedRequirements  CustomerRequirement[] @relation("Estimator")
  customerRequirements   CustomerRequirement[]
  installationOrders     InstallationOrder[]
  assignedMarketingLeads MarketingLead[]       @relation("AssignedLeads")
  marketingLeads         MarketingLead[]
  monthlyTargets         MonthlyTarget[]
  notifications          Notification[]
  orders                 Order[]
  managedProjects        Project[]             @relation("ProjectManager")
  reportedDailyLogs      ProjectDailyLog[]
  projectMembers         ProjectMember[]
  assignedTasks          ProjectTask[]
  pushSubscriptions      PushSubscription[]
  quotations             Quotation[]
  schedules              Schedule[]
  serviceSchedules       ServiceSchedule[]
  estimatedSurveys       SiteSurvey[]          @relation("SurveyEstimator")
  siteSurveys            SiteSurvey[]
  telesales              Telesale[]
  telesalesKPIs          TelesalesKPI[]
  employeeSale           EmployeeSale?
  jobStepLogs            JobStepLog[]
}

model MonthlyTarget {
  id        String   @id @default(cuid())
  userId    String?
  month     Int
  year      Int
  amount    Float    @default(0)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt @db.Timestamptz(6)
  user      User?    @relation(fields: [userId], references: [id])

  @@unique([userId, month, year])
  @@index([month, year])
}

model Schedule {
  id                 String    @id @default(cuid())
  userId             String
  title              String
  description        String?
  date               DateTime  @db.Timestamptz(6)
  status             String    @default("Planned")
  presentationStatus String?
  quotationNumber    String?
  poNumber           String?
  invoiceNumber      String?
  notes              String?
  visitReport        String?
  createdAt          DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt          DateTime  @updatedAt @db.Timestamptz(6)
  companyId          String?
  telesaleId         String?   @unique
  company            Company?  @relation(fields: [companyId], references: [id])
  telesale           Telesale? @relation(fields: [telesaleId], references: [id])
  user               User      @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([companyId])
  @@index([date])
}

model EmployeeSale {
  id         String    @id @default(cuid())
  userId     String?   @unique
  employeeId String?   @unique
  fullName   String
  createdAt  DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt  DateTime  @default(now()) @updatedAt @db.Timestamptz(6)
  nickname   String?
  branch     String?
  teamLeader String?
  position   String?
  department String?
  startDate  DateTime? @db.Timestamptz(6)
  user       User?     @relation(fields: [userId], references: [id])

  @@map("employee_sales")
}

model Company {
  id                    String               @id @default(cuid())
  companyName           String
  taxId                 String?
  address               String?
  createdAt             DateTime             @default(now()) @db.Timestamptz(6)
  updatedAt             DateTime             @updatedAt @db.Timestamptz(6)
  area                  String?
  branchOrHeadOffice    String?
  businessType          String?
  customerAccessChannel String?
  customerStatus        String?
  customerType          String?
  district              String?
  postalCode            String?
  province              String?
  subDistrict           String?
  assignedUserId        String?
  billingAddress        String?
  billingSubDistrict    String?
  billingDistrict       String?
  billingProvince       String?
  billingPostalCode     String?
  shippingAddress       String?
  shippingSubDistrict   String?
  shippingDistrict      String?
  shippingProvince      String?
  shippingPostalCode    String?
  paymentMethod         String?
  region                String?
  segment               String?
  tags                  String[]
  assignedUser          User?                @relation("AssignedCompany", fields: [assignedUserId], references: [id])
  interactions          CompanyInteraction[]
  contacts              Contact[]
  orders                Order[]
  quotations            Quotation[]
  schedules             Schedule[]
  siteSurveys           SiteSurvey[]
  telesales             Telesale[]

  @@index([companyName])
  @@index([assignedUserId])
}

model CompanyInteraction {
  id          String   @id @default(cuid())
  companyId   String
  userId      String
  type        String
  title       String
  description String?
  occurredAt  DateTime
  createdAt   DateTime @default(now())
  company     Company  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Contact {
  id             String      @id @default(cuid())
  companyId      String
  contactName    String
  position       String?
  mobilePhone    String?
  email          String?
  isETaxReceiver Boolean     @default(false)
  createdAt      DateTime    @default(now()) @db.Timestamptz(6)
  updatedAt      DateTime    @updatedAt @db.Timestamptz(6)
  company        Company     @relation(fields: [companyId], references: [id])
  quotations     Quotation[]

  @@index([companyId])
  @@index([contactName])
}

model Quotation {
  id                   String         @id @default(cuid())
  companyId            String
  status               String         @default("Pending")
  salesBeforeVat       Float?
  transportationFee    Float?
  installationFee      Float?
  totalAmountBeforeVat Float?
  actualClosingAmount  Float?
  createdAt            DateTime       @default(now()) @db.Timestamptz(6)
  updatedAt            DateTime       @updatedAt @db.Timestamptz(6)
  billingDate          DateTime?      @db.Timestamptz(6)
  appointmentDate      DateTime?      @db.Timestamptz(6)
  appointmentNote      String?
  contactId            String?
  followUp1            DateTime?      @db.Timestamptz(6)
  followUp2            DateTime?      @db.Timestamptz(6)
  followUp3            DateTime?      @db.Timestamptz(6)
  followUp4            DateTime?      @db.Timestamptz(6)
  invoiceNumber        String?
  poDate               DateTime?      @db.Timestamptz(6)
  poNumber             String?
  productType          String?
  quotationDate        DateTime?      @db.Timestamptz(6)
  quotationNumber      String?
  rejectReason         String?
  remarks              String?
  requirementDate      DateTime?      @db.Timestamptz(6)
  requirementNumber    String?
  salesBranch          String?
  salesTeamLeader      String?
  salespersonId        String?
  subject              String?
  winLossReason        String?
  siteSurveyId         String?
  marketingLead        MarketingLead?
  orders               Order[]
  company              Company        @relation(fields: [companyId], references: [id])
  contact              Contact?       @relation(fields: [contactId], references: [id])
  salesperson          User?          @relation(fields: [salespersonId], references: [id])
  siteSurvey           SiteSurvey?    @relation(fields: [siteSurveyId], references: [id])
  jobs                 Job[]

  @@index([companyId])
  @@index([salespersonId])
  @@index([status])
  @@index([createdAt])
  @@index([companyId, createdAt])
}

model Telesale {
  id                  String    @id @default(cuid())
  companyId           String
  userId              String?
  conversationSummary String?
  needsOrProblems     String?
  meetingObjective    String?
  competitorName      String?
  competitorPrice     Float?
  competitorPromotion String?
  lastMeetingDate     DateTime? @db.Timestamptz(6)
  result              String?
  createdAt           DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt           DateTime  @updatedAt @db.Timestamptz(6)
  callDate            DateTime? @db.Timestamptz(6)
  callOutcome         String?
  callStatus          String?
  forwardTo           String?
  callbackAt          DateTime? @db.Timestamptz(6)
  visitDate           DateTime? @db.Timestamptz(6)
  schedule            Schedule?
  company             Company   @relation(fields: [companyId], references: [id])
  user                User?     @relation(fields: [userId], references: [id])

  @@index([companyId])
  @@index([userId])
  @@index([lastMeetingDate])
  @@index([createdAt])
  @@index([companyId, createdAt])
}

model BusinessType {
  id        String   @id @default(cuid())
  name      String   @unique
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt @db.Timestamptz(6)
}

model PostalData {
  id          String   @id @default(cuid())
  postalCode  String
  subDistrict String
  district    String
  province    String
  createdAt   DateTime @default(now()) @db.Timestamptz(6)
  updatedAt   DateTime @updatedAt @db.Timestamptz(6)

  @@index([postalCode])
  @@index([province, district, subDistrict])
}

model Competitor {
  id        String   @id @default(cuid())
  name      String   @unique
  price     Float?
  promotion String?
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt @db.Timestamptz(6)

  @@index([name])
}

model TelesalesKPI {
  id                String   @id @default(cuid())
  userId            String?
  month             Int
  year              Int
  weeklyCallGoal    Int      @default(300)
  monthlyCallGoal   Int      @default(1200)
  appointmentGoal   Int      @default(20)
  connectionRateMin Float    @default(0.6)
  createdAt         DateTime @default(now()) @db.Timestamptz(6)
  updatedAt         DateTime @updatedAt @db.Timestamptz(6)
  user              User?    @relation(fields: [userId], references: [id])

  @@unique([userId, month, year])
}

model Order {
  id                 String           @id @default(cuid())
  orderNumber        String           @unique
  companyId          String
  quotationId        String?
  salespersonId      String?
  status             String           @default("เน€เธเธเน€เธเธเน€เธเธเน€เธเธ—เน€เธยเน€เธเธเน€เธเธ‘เน€เธย")
  value              Float            @default(0)
  priority           String           @default("Normal")
  targetDeliveryDate DateTime?        @db.Timestamptz(6)
  createdAt          DateTime         @default(now()) @db.Timestamptz(6)
  updatedAt          DateTime         @updatedAt @db.Timestamptz(6)
  company            Company          @relation(fields: [companyId], references: [id])
  quotation          Quotation?       @relation(fields: [quotationId], references: [id])
  salesperson        User?            @relation(fields: [salespersonId], references: [id])
  statusLogs         OrderStatusLog[]

  @@index([companyId])
  @@index([quotationId])
  @@index([salespersonId])
  @@index([status])
}

model OrderStatusLog {
  id         String   @id @default(cuid())
  orderId    String
  userId     String?
  fromStatus String
  toStatus   String
  createdAt  DateTime @default(now()) @db.Timestamptz(6)
  order      Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)

  @@index([orderId])
}

model Job {
  id                 String              @id @default(cuid())
  jobNumber          String              @unique
  companyCode        String
  jobType            String              @default("")
  month              Int
  yearBe             Int
  dateClosed         DateTime            @db.Timestamptz(6)
  customerName       String
  item               String?
  quotationNumber    String?
  poNumber           String?
  sellerName         String?
  quotationId        String?
  currentStep        String              @default("sales") @map("current_step")
  flowVariant        String?             @map("flow_variant")
  createdAt          DateTime            @default(now()) @db.Timestamptz(6)
  updatedAt          DateTime            @updatedAt @db.Timestamptz(6)
  courierCompany     String?
  deliveryDate       DateTime?           @db.Timestamptz(6)
  deliveryMethod     String?
  trackingNumber     String?
  trackingPhotoUrl   String?
  paymentMethod      String?
  paymentStatus      String?             @default("pending")
  billingRegulations String?
  creditDocsUrl      String?
  creditTerms        String?
  percentageTerms    String?
  salesOrderDate     DateTime?           @db.Timestamptz(6)
  paymentDate        DateTime?           @db.Timestamptz(6)
  billingDocsUrl     String?
  installationOrders InstallationOrder[]
  outsourceRepairs   OutsourceRepair[]
  project            Project?
  repairDeliveries   RepairDelivery[]
  repairOrder        RepairOrder?
  stepLogs           JobStepLog[]
  quotation          Quotation?          @relation(fields: [quotationId], references: [id])
  paymentTasks       PaymentTask[]

  @@map("jobs")
}

model PaymentTask {
  id                String    @id @default(cuid())
  jobId             String
  status            String    @default("เธฃเธญเธ”เธณเน€เธเธดเธเธเธฒเธฃ")
  assignedTo        String?
  dueDate           DateTime? @db.Timestamptz(6)
  paidAmount        Float?
  paidDate          DateTime? @db.Timestamptz(6)
  note              String?
  createdAt         DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt         DateTime  @updatedAt @db.Timestamptz(6)
  installmentAmount Float?
  installmentNo     Int?
  installmentTotal  Int?
  job               Job       @relation(fields: [jobId], references: [id], onDelete: Cascade)

  @@map("payment_tasks")
}

model JobRunningNumber {
  id         String @id @default(cuid())
  yearBe     Int
  month      Int
  lastNumber Int    @default(0)

  @@unique([yearBe, month])
  @@map("job_running_numbers")
}

model JobStepLog {
  id                String   @id @default(cuid())
  jobId             String   @map("job_id")
  step              String
  completedBy       String   @map("completed_by")
  department        String
  note              String?
  completedAt       DateTime @default(now()) @map("completed_at") @db.Timestamptz(6)
  completedByUserId String?
  completedByUser   User?    @relation(fields: [completedByUserId], references: [id])
  job               Job      @relation(fields: [jobId], references: [id], onDelete: Cascade)

  @@map("job_step_logs")
}

model RepairOrder {
  id              String    @id @default(cuid())
  jobId           String    @unique
  invoiceNo       String?
  deliveryMethod  String?
  deliveryNoteNo  String?
  receiverName    String?
  senderName      String?
  handoverRef     String?
  phoneNumber     String?
  workType        String?
  forwardedBy     String?
  items           Json
  symptoms        String?
  settings        String?
  checklist       Json
  receivedDate    DateTime? @db.Timestamptz(6)
  sentDate        DateTime? @db.Timestamptz(6)
  createdAt       DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt       DateTime  @updatedAt @db.Timestamptz(6)
  checklistImages Json?
  company         String?
  customerAddress String?
  customerCompany String?
  salesPerson     String?
  technicianName  String?
  job             Job       @relation(fields: [jobId], references: [id], onDelete: Cascade)
}

model RepairDelivery {
  id                  String    @id @default(cuid())
  deliveryNumber      String    @unique
  note                String?
  createdAt           DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt           DateTime  @updatedAt @db.Timestamptz(6)
  address             String?
  company             String?
  customer            String?
  customerPosition    String?
  deliveryDate        DateTime? @db.Timestamptz(6)
  jobId               String?
  jobName             String?
  nameReceiver        String?
  nameSender          String?
  pdfUrl              String?
  quotationNo         String?
  sender              String?
  senderPhone         String?
  sigReceiverUrl      String?
  sigSenderUrl        String?
  siteAddress         String?
  technician          String?
  technicianPhone     String?
  workInspect         Boolean   @default(false)
  workInstall         Boolean   @default(false)
  workOther           String?
  workRepair          Boolean   @default(false)
  workTraining        Boolean   @default(false)
  status              String    @default("Draft")
  workInspectDetails  String?
  workInstallDetails  String?
  workRepairDetails   String?
  workTrainingDetails String?
  internalCompany     String?
  job                 Job?      @relation(fields: [jobId], references: [id], onDelete: Cascade)
}

model InstallationOrder {
  id               String    @id @default(cuid())
  installationNo   String    @unique
  jobId            String?
  installationDate DateTime? @db.Timestamptz(6)
  company          String?
  jobName          String?
  customer         String?
  customerPosition String?
  address          String?
  siteAddress      String?
  quotationNo      String?
  sender           String?
  senderPhone      String?
  technician       String?
  technicianPhone  String?
  items            Json?
  checklist        Json?
  checklistImages  Json?
  note             String?
  sigSenderUrl     String?
  nameSender       String?
  sigReceiverUrl   String?
  nameReceiver     String?
  status           String    @default("Draft")
  pdfUrl           String?
  createdAt        DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt        DateTime  @updatedAt @db.Timestamptz(6)
  planUpdatedAt    DateTime? @db.Timestamptz(6)
  planUpdatedBy    String?
  plannedEndDate   DateTime? @db.Timestamptz(6)
  plannedStartDate DateTime? @db.Timestamptz(6)
  technicianNote   String?
  workLocation     String?
  workPlan         String?
  technicianUserId String?
  job              Job?      @relation(fields: [jobId], references: [id], onDelete: Cascade)
  technicianUser   User?     @relation(fields: [technicianUserId], references: [id])
}

model OutsourceRepair {
  id                 String    @id @default(cuid())
  outsourceNumber    String?   @unique
  jobId              String?
  vendorName         String?
  vendorPhone        String?
  vendorAddress      String?
  customerName       String?
  customerPhone      String?
  customerAddress    String?
  sentDate           DateTime? @db.Timestamptz(6)
  expectedReturnDate DateTime? @db.Timestamptz(6)
  items              Json
  symptoms           String?
  settings           String?
  remark             String?
  sender             String?
  status             String    @default("SENT")
  createdAt          DateTime  @default(now()) @db.Timestamptz(6)
  updatedAt          DateTime  @updatedAt @db.Timestamptz(6)
  job                Job?      @relation(fields: [jobId], references: [id])
}

model CustomerRequirement {
  id                String    @id @default(cuid())
  userId            String?
  companyName       String
  contactName       String
  salesperson       String
  date              DateTime  @db.Timestamptz(6)
  formData          Json
  createdAt         DateTime  @default(now()) @db.Timestamptz(6)
  requirementNumber String?   @unique
  estimatedAt       DateTime? @db.Timestamptz(6)
  estimatedBy       String?
  estimatedPrice    Float?
  estimationNote    String?
  estimationStatus  String?
  isSentToService   Boolean   @default(false)
  assignedTo        String?
  assignedToUserId  String?
  estimationDueDate DateTime? @db.Timestamptz(6)
  boqNumber         String?   @unique
  estimatedByUserId String?
  estimatedByUser   User?     @relation("Estimator", fields: [estimatedByUserId], references: [id])
  user              User?     @relation(fields: [userId], references: [id])

  @@index([companyName])
  @@index([userId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admin_login_attempts {
  id         BigInt   @id @default(autoincrement())
  username   String   @db.VarChar(50)
  ip         String   @db.VarChar(64)
  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@index([ip, created_at], map: "idx_admin_attempts_ip_time")
  @@index([username, created_at], map: "idx_admin_attempts_user_time")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admins {
  id            Int             @id @default(autoincrement())
  username      String          @unique @db.VarChar(50)
  password_hash String
  full_name     String?         @db.VarChar(100)
  last_login    DateTime?       @db.Timestamptz(6)
  created_at    DateTime        @default(now()) @db.Timestamptz(6)
  role          String          @default("SUPER_ADMIN") @db.VarChar(20)
  announcements announcements[]
  sessions      sessions[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model asset_borrowings {
  id                       Int       @id @default(autoincrement())
  asset_id                 Int
  emp_id                   String    @db.VarChar(20)
  borrow_date              DateTime  @db.Timestamptz(6)
  expected_return_date     DateTime  @db.Timestamptz(6)
  actual_return_date       DateTime? @db.Timestamptz(6)
  location                 String?   @db.VarChar(255)
  condition_at_borrow      String?
  condition_at_return      String?
  is_damaged               Boolean   @default(false)
  photo_url_borrow         String?
  photo_url_return         String?
  status                   String    @default("borrowed") @db.VarChar(20)
  created_at               DateTime  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime  @default(now()) @db.Timestamptz(6)
  borrow_inspection_remark String?
  borrow_is_body_ok        Boolean?
  borrow_is_clean          Boolean?
  borrow_is_insurance_ok   Boolean?
  borrow_is_lights_ok      Boolean?
  borrow_is_tires_ok       Boolean?
  borrow_vehicle_status    String?   @db.VarChar(50)
  quantity                 Int       @default(1)
  assets                   assets    @relation(fields: [asset_id], references: [id], onDelete: Cascade)
  employees                employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@index([asset_id])
  @@index([emp_id])
  @@index([status])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assets {
  id               Int                @id @default(autoincrement())
  asset_id         String             @unique @db.VarChar(50)
  name             String             @db.VarChar(200)
  category         String?            @db.VarChar(100)
  status           String             @default("available") @db.VarChar(20)
  description      String?
  created_at       DateTime           @default(now()) @db.Timestamptz(6)
  updated_at       DateTime           @default(now()) @db.Timestamptz(6)
  brand            String?            @db.VarChar(100)
  company_owner    String?            @db.VarChar(100)
  main_user        String?            @db.VarChar(200)
  usage_remark     String?
  vehicle_model    String?            @db.VarChar(200)
  vehicle_type     String?            @db.VarChar(100)
  image_url        String?
  stock            Int                @default(50)
  asset_borrowings asset_borrowings[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model birthday_claims {
  id                    String    @id @db.VarChar(30)
  emp_id                String    @db.VarChar(20)
  name                  String    @db.VarChar(100)
  amount_cash           Decimal   @db.Decimal(12, 2)
  amount_meal           Decimal   @db.Decimal(12, 2)
  receipt_url           String?
  transfer_slip_url     String?
  celebration_photo_url String?
  status                String    @default("pending") @db.VarChar(20)
  created_at            DateTime  @default(now()) @db.Timestamptz(6)
  approved_by           String?   @db.VarChar(100)
  approved_at           DateTime? @db.Timestamptz(6)
  substitute_date       DateTime? @db.Date
  is_sales              Boolean   @default(false)
  employees             employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model branches {
  id         String      @id @db.VarChar(10)
  name       String      @db.VarChar(100)
  center_lat Decimal     @db.Decimal(10, 8)
  center_lon Decimal     @db.Decimal(11, 8)
  radius_m   Int
  created_at DateTime    @default(now()) @db.Timestamptz(6)
  updated_at DateTime    @default(now()) @db.Timestamptz(6)
  employees  employees[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model checkins {
  id           BigInt   @id @default(autoincrement())
  timestamp    DateTime @default(now()) @db.Timestamptz(6)
  date_key     DateTime @db.Date
  time_key     DateTime @db.Time(6)
  emp_id       String   @db.VarChar(50)
  name         String   @db.VarChar(200)
  type         String   @db.VarChar(50)
  branch_name  String   @db.VarChar(200)
  lat          Decimal? @db.Decimal(10, 8)
  lon          Decimal? @db.Decimal(11, 8)
  accuracy     Int?
  distance     Int?
  capture_mode String   @default("webrtc") @db.VarChar(50)
  late_status  String?  @db.VarChar(50)
  late_min     Int?
  photo_url    String?
  project_name String?  @db.VarChar(200)
  remark       String?
  customer_id  Int?
  is_trip      Boolean? @default(false)

  @@index([branch_name], map: "idx_checkins_branch")
  @@index([date_key], map: "idx_checkins_date")
  @@index([emp_id, date_key], map: "idx_checkins_emp_date")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model commission_claims {
  id                     String    @id
  emp_id                 String    @db.VarChar(20)
  date                   DateTime  @db.Date
  customer_name          String    @db.VarChar(200)
  companion_ids          String[]  @db.VarChar(20)
  selling_price          Decimal?  @db.Decimal(12, 2)
  commission_rate        Decimal?  @default(0.01) @db.Decimal(5, 4)
  total_commission       Decimal?  @db.Decimal(12, 2)
  per_person_commission  Decimal?  @db.Decimal(12, 2)
  status                 String    @default("pending_supervisor") @db.VarChar(50)
  supervisor_id          String?   @db.VarChar(20)
  supervisor_approved_at DateTime? @db.Timestamptz(6)
  approved_by            String?   @db.VarChar(100)
  approved_at            DateTime? @db.Timestamptz(6)
  created_at             DateTime  @default(now()) @db.Timestamptz(6)
  employees              employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model daily_work_plans {
  id                 Int       @id @default(autoincrement())
  emp_id             String    @db.VarChar(20)
  date               DateTime  @db.Date
  morning_plan       String
  morning_location   String    @db.VarChar(200)
  afternoon_plan     String
  afternoon_location String    @db.VarChar(200)
  ot_plan            String?
  ot_location        String?   @db.VarChar(200)
  ot_attendant       String?   @db.VarChar(100)
  created_at         DateTime  @default(now()) @db.Timestamptz(6)
  notified_at        DateTime? @db.Timestamptz(6)
  employees          employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@unique([emp_id, date])
  @@index([emp_id, date])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model departments {
  id            Int             @id @default(autoincrement())
  name          String          @db.VarChar(100)
  created_at    DateTime        @default(now()) @db.Timestamptz(6)
  updated_at    DateTime        @default(now()) @db.Timestamptz(6)
  division_id   Int?
  divisions     divisions?      @relation(fields: [division_id], references: [id], onUpdate: NoAction)
  employees     employees[]
  job_positions job_positions[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model divisions {
  id          Int           @id @default(autoincrement())
  name        String        @db.VarChar(100)
  created_at  DateTime      @default(now()) @db.Timestamptz(6)
  updated_at  DateTime      @default(now()) @db.Timestamptz(6)
  departments departments[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_warnings {
  id         Int       @id @default(autoincrement())
  emp_id     String    @db.VarChar(20)
  date       DateTime  @default(now()) @db.Date
  reason     String
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  employees  employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@index([date])
  @@index([emp_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employees {
  emp_id                                                               String                      @id @db.VarChar(20)
  name                                                                 String                      @db.VarChar(100)
  branch_id                                                            String?                     @db.VarChar(10)
  is_active                                                            Boolean                     @default(true)
  created_at                                                           DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                                                           DateTime                    @default(now()) @db.Timestamptz(6)
  pin_hash                                                             String?
  gender                                                               String?                     @db.VarChar(1)
  hire_date                                                            DateTime?                   @db.Date
  birth_date                                                           DateTime?                   @db.Date
  phone_number                                                         String?                     @db.VarChar(20)
  base_salary                                                          Decimal?                    @db.Decimal(12, 2)
  department_id                                                        Int?
  job_position_id                                                      Int?
  otp_code                                                             String?                     @db.VarChar(6)
  otp_expires_at                                                       DateTime?                   @db.Timestamptz(6)
  supervisor_id                                                        String?                     @db.VarChar(20)
  bank_account_no                                                      String?                     @db.VarChar(50)
  bank_name                                                            String?                     @db.VarChar(100)
  is_on_trial                                                          Boolean                     @default(false)
  has_telephone_allowance                                              Boolean                     @default(false)
  position_allowance                                                   Decimal?                    @default(0) @db.Decimal(12, 2)
  address                                                              String?
  national_id_card                                                     String?                     @db.VarChar(20)
  salary_type                                                          String?                     @default("monthly") @db.VarChar(20)
  line_user_id                                                         String?                     @unique @db.VarChar(50)
  is_checkin_exempt                                                    Boolean                     @default(false)
  probation_end_date                                                   DateTime?                   @db.Date
  resignation_date                                                     DateTime?                   @db.Date
  secondary_supervisor_id                                              String?                     @db.VarChar(20)
  nickname                                                             String?                     @db.VarChar(50)
  email                                                                String?                     @db.VarChar(100)
  general_allowance                                                    Decimal?                    @default(0) @db.Decimal(12, 2)
  probation_accommodation_allowance                                    Boolean                     @default(false)
  probation_meal_allowance                                             Boolean                     @default(false)
  probation_travel_allowance                                           Boolean                     @default(false)
  fixed_accommodation_allowance                                        Decimal?                    @default(0) @db.Decimal(12, 2)
  fixed_meal_allowance                                                 Decimal?                    @default(0) @db.Decimal(12, 2)
  fixed_travel_allowance                                               Decimal?                    @default(0) @db.Decimal(12, 2)
  fixed_tax_deduction                                                  Decimal?                    @db.Decimal(10, 2)
  current_streak                                                       Int                         @default(0)
  sso_include_fixed_accommodation                                      Boolean                     @default(false)
  sso_include_fixed_meal                                               Boolean                     @default(false)
  sso_include_fixed_travel                                             Boolean                     @default(false)
  sso_include_general_allowance                                        Boolean                     @default(false)
  sso_include_position_allowance                                       Boolean                     @default(true)
  car_benefit                                                          Decimal?                    @db.Decimal(10, 2)
  housing_benefit                                                      Decimal?                    @db.Decimal(10, 2)
  provident_fund_amt                                                   Decimal?                    @db.Decimal(10, 2)
  provident_fund_rate                                                  Decimal?                    @db.Decimal(5, 2)
  tax_deduction_override                                               Decimal?                    @db.Decimal(10, 2)
  asset_borrowings                                                     asset_borrowings[]
  birthday_claims                                                      birthday_claims[]
  clothing_requests                                                    clothing_requests[]
  coin_exchanges                                                       coin_exchanges[]
  coin_ledgers                                                         coin_ledgers[]
  coin_transfers_coin_transfers_receiver_emp_idToemployees             coin_transfers[]            @relation("coin_transfers_receiver_emp_idToemployees")
  coin_transfers_coin_transfers_sender_emp_idToemployees               coin_transfers[]            @relation("coin_transfers_sender_emp_idToemployees")
  commission_claims                                                    commission_claims[]
  daily_work_plans                                                     daily_work_plans[]
  employee_coins                                                       employee_coins[]
  employee_trainings_employee_trainings_assessor_idToemployees         employee_trainings[]        @relation("employee_trainings_assessor_idToemployees")
  employee_trainings_employee_trainings_emp_idToemployees              employee_trainings[]        @relation("employee_trainings_emp_idToemployees")
  employee_warnings                                                    employee_warnings[]
  departments                                                          departments?                @relation(fields: [department_id], references: [id], onUpdate: NoAction)
  job_positions                                                        job_positions?              @relation(fields: [job_position_id], references: [id], onUpdate: NoAction)
  employees_employees_secondary_supervisor_idToemployees               employees?                  @relation("employees_secondary_supervisor_idToemployees", fields: [secondary_supervisor_id], references: [emp_id], onUpdate: NoAction)
  other_employees_employees_secondary_supervisor_idToemployees         employees[]                 @relation("employees_secondary_supervisor_idToemployees")
  employees_employees_supervisor_idToemployees                         employees?                  @relation("employees_supervisor_idToemployees", fields: [supervisor_id], references: [emp_id], onUpdate: NoAction)
  other_employees_employees_supervisor_idToemployees                   employees[]                 @relation("employees_supervisor_idToemployees")
  branches                                                             branches?                   @relation(fields: [branch_id], references: [id], onUpdate: NoAction, map: "fk_employees_branch")
  general_welfare_claims                                               general_welfare_claims[]
  kpi_evaluations_kpi_evaluations_emp_idToemployees                    kpi_evaluations[]           @relation("kpi_evaluations_emp_idToemployees")
  kpi_evaluations_kpi_evaluations_supervisor_idToemployees             kpi_evaluations[]           @relation("kpi_evaluations_supervisor_idToemployees")
  leave_requests                                                       leave_requests[]
  monthly_payroll_data                                                 monthly_payroll_data[]
  ot_requests                                                          ot_requests[]
  probation_evaluations_probation_evaluations_emp_idToemployees        probation_evaluations[]     @relation("probation_evaluations_emp_idToemployees")
  probation_evaluations_probation_evaluations_supervisor_idToemployees probation_evaluations[]     @relation("probation_evaluations_supervisor_idToemployees")
  product_borrowings                                                   product_borrowings[]
  reward_redemptions_reward_redemptions_emp_idToemployees              reward_redemptions[]        @relation("reward_redemptions_emp_idToemployees")
  reward_redemptions_reward_redemptions_processed_byToemployees        reward_redemptions[]        @relation("reward_redemptions_processed_byToemployees")
  room_booking_attendees                                               room_booking_attendees[]
  room_bookings                                                        room_bookings[]
  task_assignments                                                     task_assignments[]
  task_coin_budgets                                                    task_coin_budgets?
  tasks                                                                tasks[]
  transfer_budgets                                                     transfer_budgets?
  travel_claims                                                        travel_claims[]
  withholding_tax_documents                                            withholding_tax_documents[]

  @@index([is_active], map: "idx_employees_active")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model general_welfare_claims {
  id                     String    @id @db.VarChar(30)
  emp_id                 String    @db.VarChar(20)
  welfare_type           String    @db.VarChar(50)
  amount                 Decimal   @db.Decimal(12, 2)
  attachment_url         String?
  remark                 String?
  status                 String    @default("pending") @db.VarChar(20)
  created_at             DateTime  @default(now()) @db.Timestamptz(6)
  approved_by            String?   @db.VarChar(100)
  approved_at            DateTime? @db.Timestamptz(6)
  admin_comment          String?
  metadata               Json?
  supervisor_approved_at DateTime? @db.Timestamptz(6)
  supervisor_approved_by String?   @db.VarChar(100)
  supervisor_status      String    @default("pending") @db.VarChar(20)
  employees              employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@index([emp_id])
  @@index([status])
  @@index([welfare_type])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model holidays {
  date       DateTime @id @db.Date
  name       String   @db.VarChar(100)
  created_at DateTime @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_positions {
  id                  Int             @id @default(autoincrement())
  department_id       Int?
  title               String          @db.VarChar(100)
  is_ot_eligible      Boolean         @default(true)
  created_at          DateTime        @default(now()) @db.Timestamptz(6)
  updated_at          DateTime        @default(now()) @db.Timestamptz(6)
  node_type           String?         @default("staff") @db.VarChar(20)
  order_index         Int?            @default(0)
  parent_id           Int?
  employees           employees[]
  departments         departments?    @relation(fields: [department_id], references: [id], onDelete: Cascade)
  job_positions       job_positions?  @relation("job_positionsTojob_positions", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_job_positions job_positions[] @relation("job_positionsTojob_positions")

  @@index([department_id])
  @@index([parent_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model kpi_evaluations {
  id                                                 Int         @id @default(autoincrement())
  emp_id                                             String      @db.VarChar(20)
  supervisor_id                                      String      @db.VarChar(20)
  evaluation_no                                      Int         @default(1)
  evaluation_date                                    DateTime    @default(now()) @db.Date
  period_start                                       DateTime?   @db.Date
  period_end                                         DateTime?   @db.Date
  status                                             String      @default("draft") @db.VarChar(50)
  employee_comment                                   String?
  supervisor_comment                                 String?
  total_employee_score                               Decimal?    @db.Decimal(5, 2)
  total_supervisor_score                             Decimal?    @db.Decimal(5, 2)
  grade                                              String?     @db.VarChar(2)
  created_at                                         DateTime    @default(now()) @db.Timestamptz(6)
  updated_at                                         DateTime    @default(now()) @db.Timestamptz(6)
  category                                           String      @default("PROBATION") @db.VarChar(20)
  session_name                                       String?     @db.VarChar(50)
  year                                               Int         @default(2026)
  is_passing                                         Boolean?    @default(false)
  recommend_salary                                   Boolean?    @default(false)
  employees_kpi_evaluations_emp_idToemployees        employees   @relation("kpi_evaluations_emp_idToemployees", fields: [emp_id], references: [emp_id])
  employees_kpi_evaluations_supervisor_idToemployees employees   @relation("kpi_evaluations_supervisor_idToemployees", fields: [supervisor_id], references: [emp_id])
  kpi_items                                          kpi_items[]

  @@index([emp_id])
  @@index([supervisor_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model kpi_items {
  id                 Int             @id @default(autoincrement())
  kpi_evaluation_id  Int
  objective          String
  indicator          String
  weight             Decimal         @db.Decimal(5, 2)
  result_description String?
  employee_score     Int?
  supervisor_score   Int?
  target_1           String?
  target_2           String?
  target_3           String?
  target_4           String?
  target_5           String?
  section            String          @default("KPI") @db.VarChar(50)
  kpi_evaluations    kpi_evaluations @relation(fields: [kpi_evaluation_id], references: [id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model leave_entitlements {
  id            Int         @id @default(autoincrement())
  leave_type_id String      @db.VarChar(20)
  min_years     Int
  days          Int
  leave_types   leave_types @relation(fields: [leave_type_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([leave_type_id, min_years])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model leave_requests {
  id                     String    @id @db.VarChar(30)
  timestamp              DateTime  @default(now()) @db.Timestamptz(6)
  emp_id                 String    @db.VarChar(20)
  name                   String    @db.VarChar(100)
  leave_type_id          String    @db.VarChar(20)
  leave_type             String    @db.VarChar(50)
  start_date             DateTime  @db.Date
  end_date               DateTime  @db.Date
  days                   Int
  reason                 String?
  status                 String    @default("pending") @db.VarChar(20)
  approved_by            String?   @db.VarChar(100)
  approved_at            DateTime? @db.Timestamptz(6)
  start_at               DateTime  @db.Timestamptz(6)
  end_at                 DateTime  @db.Timestamptz(6)
  minutes                Int
  attachment_url         String?
  supervisor_approved_at DateTime? @db.Timestamptz(6)
  supervisor_id          String?   @db.VarChar(20)
  handover_person        String?   @db.VarChar(100)
  substitute_date        DateTime? @db.Date
  employees              employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@index([start_date, end_date], map: "idx_leave_dates")
  @@index([emp_id, status], map: "idx_leave_emp_status")
  @@index([emp_id, start_at, end_at], map: "idx_leave_emp_time")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model leave_types {
  id                 String               @id @db.VarChar(20)
  name               String               @db.VarChar(80)
  gender_rule        String               @default("ALL") @db.VarChar(10)
  max_days           Int?
  count_mode         String               @default("WORK") @db.VarChar(12)
  require_attachment Boolean              @default(false)
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  leave_entitlements leave_entitlements[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model meeting_rooms {
  id            Int             @id @default(autoincrement())
  name          String          @db.VarChar(100)
  floor         Int
  capacity      Int?
  is_active     Boolean         @default(true)
  created_at    DateTime        @default(now()) @db.Timestamptz(6)
  updated_at    DateTime        @default(now()) @db.Timestamptz(6)
  room_bookings room_bookings[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model monthly_payroll_data {
  id                               Int       @id @default(autoincrement())
  emp_id                           String    @db.VarChar(20)
  cycle_month                      Int
  cycle_year                       Int
  social_security                  Decimal?  @db.Decimal(10, 2)
  student_loan                     Decimal?  @default(0) @db.Decimal(10, 2)
  other_deductions                 Decimal?  @default(0) @db.Decimal(10, 2)
  other_benefits                   Decimal?  @default(0) @db.Decimal(10, 2)
  override_salary                  Decimal?  @db.Decimal(10, 2)
  created_at                       DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                       DateTime  @default(now()) @db.Timestamptz(6)
  accommodation_allowance_override Decimal?  @db.Decimal(10, 2)
  diligence_allowance_override     Decimal?  @db.Decimal(10, 2)
  holiday_1_x_hours_override       Decimal?  @db.Decimal(10, 2)
  holiday_3_x_hours_override       Decimal?  @db.Decimal(10, 2)
  meal_allowance_override          Decimal?  @db.Decimal(10, 2)
  normal_1_5x_hours_override       Decimal?  @db.Decimal(10, 2)
  phone_allowance_override         Decimal?  @db.Decimal(10, 2)
  position_allowance_override      Decimal?  @db.Decimal(10, 2)
  travel_accommodation_override    Decimal?  @db.Decimal(10, 2)
  travel_allowance_override        Decimal?  @db.Decimal(10, 2)
  travel_site_allowance_override   Decimal?  @db.Decimal(10, 2)
  tax                              Decimal?  @default(0) @db.Decimal(10, 2)
  unpaid_absenteeism               Decimal?  @db.Decimal(10, 2)
  bonus                            Decimal?  @default(0) @db.Decimal(10, 2)
  commissions                      Decimal?  @default(0) @db.Decimal(10, 2)
  is_published                     Boolean   @default(false)
  insurance                        Decimal?  @default(0) @db.Decimal(10, 2)
  insurance_income                 Decimal?  @default(0) @db.Decimal(10, 2)
  general_allowance_override       Decimal?  @db.Decimal(10, 2)
  car_benefit                      Decimal?  @db.Decimal(10, 2)
  housing_benefit                  Decimal?  @db.Decimal(10, 2)
  provident_fund                   Decimal?  @db.Decimal(10, 2)
  taxable_income                   Decimal?  @db.Decimal(12, 2)
  employees                        employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@unique([emp_id, cycle_month, cycle_year], map: "idx_monthly_payroll_unique")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ot_requests {
  id                Int       @id @default(autoincrement())
  emp_id            String    @db.VarChar(20)
  date_for          DateTime  @db.Date
  start_time        DateTime  @db.Timestamptz(6)
  end_time          DateTime  @db.Timestamptz(6)
  total_hours       Decimal   @db.Decimal(5, 2)
  reason            String?
  status            String    @default("pending") @db.VarChar(20)
  supervisor_id     String?   @db.VarChar(20)
  approved_at       DateTime? @db.Timestamptz(6)
  created_at        DateTime  @default(now()) @db.Timestamptz(6)
  updated_at        DateTime  @default(now()) @db.Timestamptz(6)
  approved_hours    Decimal?  @db.Decimal(5, 2)
  supervisor_remark String?
  actual_end_at     DateTime? @db.Timestamptz(6)
  actual_start_at   DateTime? @db.Timestamptz(6)
  has_discrepancy   Boolean   @default(false)
  employees         employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@index([emp_id, date_for])
  @@index([status])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model probation_evaluations {
  id                                                       Int       @id @default(autoincrement())
  emp_id                                                   String    @db.VarChar(20)
  supervisor_id                                            String    @db.VarChar(20)
  evaluation_no                                            Int       @default(1)
  evaluation_date                                          DateTime  @default(now()) @db.Date
  period_start                                             DateTime  @db.Date
  period_end                                               DateTime  @db.Date
  score_work_quality                                       Int       @default(0)
  score_work_quantity                                      Int       @default(0)
  score_dedication                                         Int       @default(0)
  score_knowledge                                          Int       @default(0)
  score_learning                                           Int       @default(0)
  score_obedience                                          Int       @default(0)
  score_responsibility                                     Int       @default(0)
  score_creativity                                         Int       @default(0)
  score_teamwork                                           Int       @default(0)
  score_discipline                                         Int       @default(0)
  score_tool_maintenance                                   Int       @default(0)
  score_participation                                      Int       @default(0)
  score_late                                               Int       @default(0)
  score_sick_leave                                         Int       @default(0)
  score_personal_leave                                     Int       @default(0)
  count_late                                               Int       @default(0)
  count_sick_leave                                         Int       @default(0)
  count_personal_leave                                     Int       @default(0)
  count_activity                                           Int       @default(0)
  total_score                                              Int       @default(0)
  grade                                                    String?   @db.VarChar(2)
  comment_supervisor                                       String?
  comment_improvement                                      String?
  comment_praise                                           String?
  decision                                                 String    @db.VarChar(50)
  salary_adjust_from                                       Decimal?  @db.Decimal(12, 2)
  salary_adjust_to                                         Decimal?  @db.Decimal(12, 2)
  status                                                   String    @default("submitted") @db.VarChar(20)
  hr_remark                                                String?
  is_sent_to_management                                    Boolean   @default(false)
  created_at                                               DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                                               DateTime  @default(now()) @db.Timestamptz(6)
  score_comments                                           Json?
  employees_probation_evaluations_emp_idToemployees        employees @relation("probation_evaluations_emp_idToemployees", fields: [emp_id], references: [emp_id], onDelete: Cascade)
  employees_probation_evaluations_supervisor_idToemployees employees @relation("probation_evaluations_supervisor_idToemployees", fields: [supervisor_id], references: [emp_id])

  @@index([emp_id])
  @@index([supervisor_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_borrowings {
  id                   Int       @id @default(autoincrement())
  product_id           Int
  emp_id               String    @db.VarChar(20)
  borrow_date          DateTime  @db.Timestamptz(6)
  expected_return_date DateTime  @db.Timestamptz(6)
  actual_return_date   DateTime? @db.Timestamptz(6)
  location             String?   @db.VarChar(255)
  condition_at_borrow  String?
  condition_at_return  String?
  is_damaged           Boolean   @default(false)
  photo_url_borrow     String?
  photo_url_return     String?
  status               String    @default("borrowed") @db.VarChar(20)
  created_at           DateTime  @default(now()) @db.Timestamptz(6)
  updated_at           DateTime  @default(now()) @db.Timestamptz(6)
  quantity             Int       @default(1)
  employees            employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
  products             products  @relation(fields: [product_id], references: [id], onDelete: Cascade)

  @@index([emp_id])
  @@index([product_id])
  @@index([status])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model products {
  id                 Int                  @id @default(autoincrement())
  product_code       String               @unique @db.VarChar(50)
  product_name       String               @db.VarChar(200)
  category           String?              @db.VarChar(100)
  company_name       String?              @db.VarChar(100)
  description        String?
  status             String               @default("available") @db.VarChar(20)
  image_url          String?
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  stock              Int                  @default(50)
  product_borrowings product_borrowings[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model projects {
  id          Int      @id @default(autoincrement())
  code        String?  @db.VarChar(50)
  name        String   @db.VarChar(200)
  client_name String?  @db.VarChar(200)
  address     String?
  is_active   Boolean  @default(true)
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @default(now()) @db.Timestamptz(6)
  contact     String?  @db.VarChar(200)
  lat         Decimal? @db.Decimal(10, 8)
  lng         Decimal? @db.Decimal(11, 8)
  phone       String?  @db.VarChar(50)
  radius_m    Int      @default(200)
  status      String   @default("CURRENT") @db.VarChar(50)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model room_booking_attendees {
  id            Int           @id @default(autoincrement())
  booking_id    Int
  emp_id        String        @db.VarChar(20)
  room_bookings room_bookings @relation(fields: [booking_id], references: [id], onDelete: Cascade)
  employees     employees     @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@unique([booking_id, emp_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model room_bookings {
  id                     Int                      @id @default(autoincrement())
  room_id                Int
  emp_id                 String                   @db.VarChar(20)
  start_time             DateTime                 @db.Timestamptz(6)
  end_time               DateTime                 @db.Timestamptz(6)
  purpose                String?
  status                 String                   @default("approved") @db.VarChar(20)
  created_at             DateTime                 @default(now()) @db.Timestamptz(6)
  minutes                String?
  meeting_link           String?
  room_booking_attendees room_booking_attendees[]
  employees              employees                @relation(fields: [emp_id], references: [emp_id])
  meeting_rooms          meeting_rooms            @relation(fields: [room_id], references: [id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id         Int      @id @default(autoincrement())
  user_id    Int
  token      String
  expires_at DateTime @db.Timestamptz(6)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  admins     admins   @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_sessions_admin")

  @@index([token], map: "idx_sessions_token")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model travel_claims {
  id                        String    @id
  emp_id                    String    @db.VarChar(20)
  date                      DateTime  @db.Date
  claim_type                String    @db.VarChar(20)
  site_name                 String    @db.VarChar(200)
  is_overnight              Boolean   @default(false)
  accommodation_amount      Decimal   @default(0) @db.Decimal(12, 2)
  accommodation_receipt_url String?
  report_url                String
  status                    String    @default("pending_supervisor") @db.VarChar(50)
  has_pre_approval          Boolean   @default(false)
  is_supervisor_shared      Boolean   @default(false)
  supervisor_id             String?   @db.VarChar(20)
  supervisor_approved_at    DateTime? @db.Timestamptz(6)
  supervisor_remark         String?
  approved_by               String?   @db.VarChar(100)
  approved_at               DateTime? @db.Timestamptz(6)
  created_at                DateTime  @default(now()) @db.Timestamptz(6)
  remark                    String?
  end_date                  DateTime? @db.Date
  employees                 employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_trainings {
  id                                                  Int        @id @default(autoincrement())
  emp_id                                              String     @db.VarChar(20)
  course_name                                         String     @db.VarChar(255)
  institution_name                                    String?    @db.VarChar(255)
  training_date_start                                 DateTime?  @db.Date
  training_date_end                                   DateTime?  @db.Date
  completion_percentage                               Decimal?   @db.Decimal(5, 2)
  effectiveness_result                                String?
  certificate_file_url                                String?
  assessor_id                                         String?    @db.VarChar(20)
  created_at                                          DateTime   @default(now()) @db.Timestamptz(6)
  updated_at                                          DateTime   @default(now()) @db.Timestamptz(6)
  employees_employee_trainings_assessor_idToemployees employees? @relation("employee_trainings_assessor_idToemployees", fields: [assessor_id], references: [emp_id])
  employees_employee_trainings_emp_idToemployees      employees  @relation("employee_trainings_emp_idToemployees", fields: [emp_id], references: [emp_id], onDelete: Cascade)
}

model PurchaseRequest {
  id             Int             @id @default(autoincrement())
  no             Int?
  recordedAt     DateTime?       @db.Timestamptz(6)
  prNumber       String          @unique
  projectName    String?
  itemList       String?
  requestedBy    String?
  note           String?
  reportedBy     String?
  createdAt      DateTime        @default(now()) @db.Timestamptz(6)
  updatedAt      DateTime        @updatedAt @db.Timestamptz(6)
  purchaseOrders PurchaseOrder[]

  @@map("purchase_requests")
}

model PurchaseOrder {
  id              Int              @id @default(autoincrement())
  no              Int?
  recordedAt      DateTime?        @db.Timestamptz(6)
  prNumber        String?
  poNumber        String           @unique
  vendorName      String?
  accountNumber   String?
  totalAmount     Decimal?         @db.Decimal(12, 2)
  depositAmount   Decimal?         @db.Decimal(12, 2)
  remainingAmount Decimal?         @db.Decimal(12, 2)
  payment1        Decimal?         @db.Decimal(12, 2)
  creditTerm      String?
  jobName         String?
  itemList        String?
  deliveryDate    DateTime?        @db.Timestamptz(6)
  note            String?
  reportedBy      String?
  receiveStatus   String?
  receivedBy      String?
  receivedAt      DateTime?        @db.Timestamptz(6)
  createdAt       DateTime         @default(now()) @db.Timestamptz(6)
  updatedAt       DateTime         @updatedAt @db.Timestamptz(6)
  goodsReceipts   GoodsReceipt[]
  purchaseRequest PurchaseRequest? @relation(fields: [prNumber], references: [prNumber])

  @@map("purchase_orders")
}

model GoodsReceipt {
  id                   String        @id @default(cuid())
  sequenceNo           Int?
  recordedAt           DateTime?     @db.Timestamptz(6)
  company              String?
  poNumber             String
  item                 String?
  quantity             Decimal?      @db.Decimal(10, 2)
  totalAmount          Decimal?      @db.Decimal(12, 2)
  creditTerm           String?
  status               String?
  targetDeliveryDate   DateTime?     @db.Timestamptz(6)
  deliveredQuantity    Decimal?      @db.Decimal(10, 2)
  receivedAt           DateTime?     @db.Timestamptz(6)
  deliveryNoteNumber   String?
  recipient            String?
  isCompleteDelivery   Boolean       @default(false)
  isIncompleteDelivery Boolean       @default(false)
  createdAt            DateTime      @default(now()) @db.Timestamptz(6)
  updatedAt            DateTime      @updatedAt @db.Timestamptz(6)
  purchaseOrder        PurchaseOrder @relation(fields: [poNumber], references: [poNumber])

  @@unique([poNumber, sequenceNo])
  @@map("goods_receipts")
}

model Project {
  id                        String             @id @default(cuid())
  projectNumber             String             @unique
  jobId                     String?            @unique
  name                      String
  description               String?
  clientName                String?
  siteAddress               String?
  managerId                 String?
  startDate                 DateTime?          @db.Timestamptz(6)
  endDate                   DateTime?          @db.Timestamptz(6)
  budget                    Float?
  status                    String             @default("Planning")
  createdAt                 DateTime           @default(now()) @db.Timestamptz(6)
  updatedAt                 DateTime           @updatedAt @db.Timestamptz(6)
  externalTechnicians       String?
  projectCategory           String?
  department                String?
  province                  String?
  district                  String?
  projectValue              Decimal?           @db.Decimal(15, 2)
  securityDeposit           Decimal?           @db.Decimal(15, 2)
  penaltyPerDay             Decimal?           @db.Decimal(15, 2)
  amountIncludingVat        Decimal?           @db.Decimal(15, 2)
  installment1              Decimal?           @db.Decimal(15, 2)
  installment2              Decimal?           @db.Decimal(15, 2)
  installment3              Decimal?           @db.Decimal(15, 2)
  installment4              Decimal?           @db.Decimal(15, 2)
  firstPayment              Decimal?           @db.Decimal(15, 2)
  secondPayment             Decimal?           @db.Decimal(15, 2)
  contractNumber            String?
  documentNumber            String?
  deliveryDocNumber         String?
  jbNumber                  String?
  certCompletionRequestNo   String?
  certRequestStatus         String?
  depositRefundRequestNo    String?
  contractSignatory         String?
  contractReturnStatus      String?
  remarks                   String?
  pathFolder                String?
  statusPictureUrl          String?
  updateCompanyProfile      Boolean            @default(false)
  projectDuration           Int?
  projectDurationUnit       String?
  contractSigningDate       DateTime?          @db.Timestamptz(6)
  deliveryDate              DateTime?          @db.Timestamptz(6)
  paymentDate               DateTime?          @db.Timestamptz(6)
  depositCollectionSchedule DateTime?          @db.Timestamptz(6)
  job                       Job?               @relation(fields: [jobId], references: [id])
  manager                   User?              @relation("ProjectManager", fields: [managerId], references: [id])
  dailyLogs                 ProjectDailyLog[]
  equipment                 ProjectEquipment[]
  members                   ProjectMember[]
  tasks                     ProjectTask[]
}

model ProjectEquipment {
  id                 String   @id @default(cuid())
  projectId          String
  name               String
  registrationNumber String?
  quantity           Int      @default(1)
  status             String   @default("เนเธเนเธเธฒเธ")
  details            String?
  createdAt          DateTime @default(now()) @db.Timestamptz(6)
  project            Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
}

model ProjectMember {
  id        String  @id @default(cuid())
  projectId String
  userId    String
  role      String
  project   Project @relation(fields: [projectId], references: [id], onDelete: Cascade)
  user      User    @relation(fields: [userId], references: [id])

  @@unique([projectId, userId])
}

model ProjectTask {
  id           String        @id @default(cuid())
  projectId    String
  title        String
  description  String?
  category     String?
  assigneeId   String?
  planStart    DateTime?     @db.Timestamptz(6)
  planEnd      DateTime?     @db.Timestamptz(6)
  actualStart  DateTime?     @db.Timestamptz(6)
  actualEnd    DateTime?     @db.Timestamptz(6)
  planPct      Float         @default(0)
  actualPct    Float         @default(0)
  weight       Float         @default(1)
  status       String        @default("Pending")
  priority     String        @default("Normal")
  order        Int           @default(0)
  parentTaskId String?
  createdAt    DateTime      @default(now()) @db.Timestamptz(6)
  updatedAt    DateTime      @updatedAt @db.Timestamptz(6)
  assignee     User?         @relation(fields: [assigneeId], references: [id])
  parentTask   ProjectTask?  @relation("SubTasks", fields: [parentTaskId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  subtasks     ProjectTask[] @relation("SubTasks")
  project      Project       @relation(fields: [projectId], references: [id], onDelete: Cascade)
}

model DailyLogWorker {
  id         String          @id @default(cuid())
  dailyLogId String
  name       String
  position   String
  hours      Float?
  status     String
  notes      String?
  dailyLog   ProjectDailyLog @relation(fields: [dailyLogId], references: [id], onDelete: Cascade)
}

model ProjectDailyLog {
  id                String           @id @default(cuid())
  projectId         String
  reportedBy        String
  date              DateTime         @db.Date
  weather           String?
  temperature       Int?
  workerCount       Int?
  workerNote        String?
  workSummary       String?
  issues            String?
  solutions         String?
  nextPlan          String?
  safetyNote        String?
  incidents         Int              @default(0)
  taskUpdates       Json?
  imageUrls         Json?
  reporterSigUrl    String?
  supervisorSigUrl  String?
  supervisorName    String?
  createdAt         DateTime         @default(now()) @db.Timestamptz(6)
  updatedAt         DateTime         @updatedAt @db.Timestamptz(6)
  delayCause        String?
  delayExpectedDate DateTime?        @db.Date
  delayResponsible  String?
  delaySeverity     String?
  tasksPerformed    Json?
  workers           DailyLogWorker[]
  project           Project          @relation(fields: [projectId], references: [id], onDelete: Cascade)
  reporter          User             @relation(fields: [reportedBy], references: [id])

  @@unique([projectId, date, reportedBy])
}

model BranchExpense {
  id            String   @id @default(cuid())
  branch        String   @db.VarChar(100)
  salespersonId String?
  expenseType   String   @db.VarChar(100)
  amount        Float    @default(0)
  date          DateTime @db.Date
  notes         String?
  createdAt     DateTime @default(now()) @db.Timestamptz(6)
  updatedAt     DateTime @default(now()) @updatedAt @db.Timestamptz(6)
  odometer      Decimal? @db.Decimal(10, 2)

  @@index([branch])
  @@index([salespersonId])
  @@index([date])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model clothing_items {
  id                Int                 @id @default(autoincrement())
  name              String              @db.VarChar(200)
  description       String?
  image_url         String?
  is_active         Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  clothing_variants clothing_variants[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model clothing_requests {
  id                Int               @id @default(autoincrement())
  emp_id            String            @db.VarChar(20)
  variant_id        Int
  quantity          Int               @default(1)
  reason            String?
  status            String            @default("pending") @db.VarChar(50)
  requested_at      DateTime          @default(now()) @db.Timestamptz(6)
  approved_by       String?           @db.VarChar(100)
  approved_at       DateTime?         @db.Timestamptz(6)
  fulfilled_at      DateTime?         @db.Timestamptz(6)
  admin_note        String?
  employees         employees         @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
  clothing_variants clothing_variants @relation(fields: [variant_id], references: [id], onDelete: Cascade)

  @@index([emp_id])
  @@index([status])
  @@index([variant_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model clothing_variants {
  id                Int                 @id @default(autoincrement())
  item_id           Int
  size              String              @db.VarChar(50)
  stock_quantity    Int                 @default(0)
  clothing_requests clothing_requests[]
  clothing_items    clothing_items      @relation(fields: [item_id], references: [id], onDelete: Cascade)

  @@unique([item_id, size])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model coin_exchange_rates {
  id                                                        Int        @id @default(autoincrement())
  from_coin_type                                            String     @db.VarChar(20)
  to_coin_type                                              String     @db.VarChar(20)
  exchange_rate                                             Int
  is_active                                                 Boolean    @default(true)
  coin_types_coin_exchange_rates_from_coin_typeTocoin_types coin_types @relation("coin_exchange_rates_from_coin_typeTocoin_types", fields: [from_coin_type], references: [id], onDelete: Cascade)
  coin_types_coin_exchange_rates_to_coin_typeTocoin_types   coin_types @relation("coin_exchange_rates_to_coin_typeTocoin_types", fields: [to_coin_type], references: [id], onDelete: Cascade)

  @@unique([from_coin_type, to_coin_type])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model coin_exchanges {
  id             Int       @id @default(autoincrement())
  emp_id         String    @db.VarChar(20)
  from_coin_type String    @db.VarChar(20)
  to_coin_type   String    @db.VarChar(20)
  amount_from    Int
  amount_to      Int
  created_at     DateTime  @default(now()) @db.Timestamptz(6)
  employees      employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model coin_ledgers {
  id               Int        @id @default(autoincrement())
  emp_id           String     @db.VarChar(20)
  coin_type_id     String     @db.VarChar(20)
  amount           Int
  transaction_type String     @db.VarChar(50)
  source_key       String?    @unique @db.VarChar(100)
  description      String?
  created_at       DateTime   @default(now()) @db.Timestamptz(6)
  coin_types       coin_types @relation(fields: [coin_type_id], references: [id], onDelete: Cascade)
  employees        employees  @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@index([emp_id])
  @@index([transaction_type])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model coin_transfers {
  id                                                  Int       @id @default(autoincrement())
  sender_emp_id                                       String    @db.VarChar(20)
  receiver_emp_id                                     String    @db.VarChar(20)
  amount                                              Int
  message                                             String?
  created_at                                          DateTime  @default(now()) @db.Timestamptz(6)
  employees_coin_transfers_receiver_emp_idToemployees employees @relation("coin_transfers_receiver_emp_idToemployees", fields: [receiver_emp_id], references: [emp_id], onDelete: Cascade)
  employees_coin_transfers_sender_emp_idToemployees   employees @relation("coin_transfers_sender_emp_idToemployees", fields: [sender_emp_id], references: [emp_id], onDelete: Cascade)

  @@index([receiver_emp_id])
  @@index([sender_emp_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model coin_types {
  id                                                                 String                @id @db.VarChar(20)
  name                                                               String                @db.VarChar(50)
  description                                                        String?
  is_active                                                          Boolean               @default(true)
  created_at                                                         DateTime              @default(now()) @db.Timestamptz(6)
  coin_exchange_rates_coin_exchange_rates_from_coin_typeTocoin_types coin_exchange_rates[] @relation("coin_exchange_rates_from_coin_typeTocoin_types")
  coin_exchange_rates_coin_exchange_rates_to_coin_typeTocoin_types   coin_exchange_rates[] @relation("coin_exchange_rates_to_coin_typeTocoin_types")
  coin_ledgers                                                       coin_ledgers[]
  employee_coins                                                     employee_coins[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_coins {
  id           Int        @id @default(autoincrement())
  emp_id       String     @db.VarChar(20)
  coin_type_id String     @db.VarChar(20)
  balance      Int        @default(0)
  updated_at   DateTime   @default(now()) @db.Timestamptz(6)
  coin_types   coin_types @relation(fields: [coin_type_id], references: [id], onDelete: Cascade)
  employees    employees  @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)

  @@unique([emp_id, coin_type_id])
  @@index([emp_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reward_redemptions {
  id                                                   Int        @id @default(autoincrement())
  emp_id                                               String     @db.VarChar(20)
  reward_id                                            Int
  quantity                                             Int        @default(1)
  status                                               String     @default("pending") @db.VarChar(50)
  redeemed_at                                          DateTime   @default(now()) @db.Timestamptz(6)
  fulfilled_at                                         DateTime?  @db.Timestamptz(6)
  cancelled_reason                                     String?
  coin_type_id                                         String?    @db.VarChar(20)
  points_spent                                         Int        @default(0)
  processed_by                                         String?    @db.VarChar(20)
  costs                                                Json?
  employees_reward_redemptions_emp_idToemployees       employees  @relation("reward_redemptions_emp_idToemployees", fields: [emp_id], references: [emp_id], onDelete: Cascade)
  employees_reward_redemptions_processed_byToemployees employees? @relation("reward_redemptions_processed_byToemployees", fields: [processed_by], references: [emp_id])
  rewards                                              rewards    @relation(fields: [reward_id], references: [id], onDelete: Cascade)

  @@index([emp_id])
  @@index([reward_id])
  @@index([status])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rewards {
  id                 Int                  @id @default(autoincrement())
  name               String               @db.VarChar(200)
  description        String?
  image_url          String?
  required_coins     Int
  required_coin_type String               @db.VarChar(20)
  stock_quantity     Int                  @default(0)
  is_active          Boolean              @default(true)
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  costs              Json?
  reward_redemptions reward_redemptions[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model task_assignments {
  id           Int       @id @default(autoincrement())
  task_id      Int
  emp_id       String    @db.VarChar(20)
  status       String    @default("PENDING") @db.VarChar(20)
  completed_at DateTime? @db.Timestamptz(6)
  completed_by String?   @db.VarChar(20)
  employees    employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
  tasks        tasks     @relation(fields: [task_id], references: [id], onDelete: Cascade)

  @@unique([task_id, emp_id])
  @@index([emp_id, status])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model task_coin_budgets {
  emp_id          String    @id @db.VarChar(20)
  used_this_month Int       @default(0)
  monthly_limit   Int       @default(20)
  reset_at        DateTime  @db.Timestamptz(6)
  employees       employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tasks {
  id               Int                @id @default(autoincrement())
  title            String             @db.VarChar(200)
  description      String?
  created_by       String             @db.VarChar(20)
  deadline         DateTime           @db.Timestamptz(6)
  created_at       DateTime           @default(now()) @db.Timestamptz(6)
  task_assignments task_assignments[]
  employees        employees          @relation(fields: [created_by], references: [emp_id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transfer_budgets {
  id            Int       @id @default(autoincrement())
  emp_id        String    @unique @db.VarChar(20)
  balance       Int       @default(20)
  monthly_topup Int       @default(20)
  updated_at    DateTime  @default(now()) @db.Timestamptz(6)
  last_topup_at DateTime? @db.Timestamptz(6)
  employees     employees @relation(fields: [emp_id], references: [emp_id], onDelete: Cascade)
}

model MarketingLead {
  id                  String     @id @default(cuid())
  customerName        String
  phoneNumber         String?
  productOfInterest   String?
  productType         String?
  conversationContent String?
  createdByUserId     String
  isForwarded         Boolean    @default(false)
  forwardedAt         DateTime?
  quotationId         String?    @unique
  createdAt           DateTime   @default(now()) @db.Timestamptz(6)
  updatedAt           DateTime   @default(now()) @updatedAt @db.Timestamptz(6)
  assignedToId        String?
  isContacted         Boolean    @default(false)
  assignedTo          User?      @relation("AssignedLeads", fields: [assignedToId], references: [id])
  createdBy           User       @relation(fields: [createdByUserId], references: [id])
  quotation           Quotation? @relation(fields: [quotationId], references: [id])
}

model Notification {
  id        String   @id @default(cuid())
  userId    String
  title     String
  message   String
  isRead    Boolean  @default(false)
  linkUrl   String?
  type      String
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  user      User     @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([createdAt])
  @@index([isRead])
}

model SiteSurvey {
  id                        String                   @id @default(cuid())
  surveyNumber              String                   @unique
  surveyDate                DateTime                 @db.Date
  companyId                 String?
  customerName              String?
  projectName               String?
  projectLocation           String?
  coordinatorName           String?
  coordinatorPhone          String?
  salespersonId             String
  surveyorName              String?
  status                    String                   @default("Draft")
  version                   Int                      @default(1)
  latitude                  Float?
  longitude                 Float?
  hasSingleLineDiagram      Boolean                  @default(false)
  requiredInfoChecklist     String[]                 @default([])
  additionalRemark          String?
  createdAt                 DateTime                 @default(now()) @db.Timestamptz(6)
  updatedAt                 DateTime                 @updatedAt @db.Timestamptz(6)
  estimatedAt               DateTime?
  estimatedByUserId         String?
  estimatedPrice            Float?
  estimationNote            String?
  estimationStatus          String?
  amrCustomerCode           String?
  buildingElectricalFileUrl String?
  buildingPlanFileUrl       String?
  electricalCabinetFileUrl  String?
  loadProfileFileUrl        String?
  roofStructureFileUrl      String?
  quotations                Quotation[]
  company                   Company?                 @relation(fields: [companyId], references: [id])
  estimatedByUser           User?                    @relation("SurveyEstimator", fields: [estimatedByUserId], references: [id])
  salesperson               User                     @relation(fields: [salespersonId], references: [id])
  electricityBill           SurveyBill?
  documents                 SurveyDocument[]
  electricalProfile         SurveyElectricalProfile?
  photos                    SurveyPhoto[]
  qa                        SurveyQA?
  structure                 SurveyStructure?
  tariffSelection           SurveyTariffSelection?
  usageBehavior             SurveyUsageBehavior?
}

model SurveyUsageBehavior {
  id                    String     @id @default(cuid())
  surveyId              String     @unique
  workingHours          String?
  workingDaysPerWeek    Int?
  workingDaysPerYear    Int?
  nonWorkingDaysPerWeek Int?
  nonWorkingDaysPerYear Int?
  dayNightRatio         String?
  avgBillThbPerMonth    Float?
  mainEquipment         String?
  survey                SiteSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
}

model SurveyElectricalProfile {
  id                    String     @id @default(cuid())
  surveyId              String     @unique
  gridProvider          String?
  userType              String?
  voltageLevel          String[]   @default([])
  meterSize             String[]   @default([])
  rateThbPerKwh         Float?
  kwhQty                Float?
  peakKwWeekday         Float?
  peakKwWeekend         Float?
  transformerCount      Int?
  transformerBrand      String?
  transformerModel      String?
  transformerKva        Float?
  breakerAmp            Float?
  mainPanelCableLengthM Float?
  amrUsernameEncrypted  String?
  amrPasswordEncrypted  String?
  survey                SiteSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
}

model SurveyTariffSelection {
  id             String             @id @default(cuid())
  surveyId       String             @unique
  tariffCategory String?
  survey         SiteSurvey         @relation(fields: [surveyId], references: [id], onDelete: Cascade)
  tiers          SurveyTariffTier[]
}

model SurveyTariffTier {
  id             String                @id @default(cuid())
  selectionId    String
  tierName       String?
  ratePerKwh     Float?
  qtyKwhPerMonth Float?
  selection      SurveyTariffSelection @relation(fields: [selectionId], references: [id], onDelete: Cascade)
}

model SurveyStructure {
  id                   String          @id @default(cuid())
  surveyId             String          @unique
  roofType             String[]        @default([])
  roofAgeYear          Int?
  roofDimWxlM          String?
  buildingHeightM      Float?
  roofPattern          String?
  metalSheetType       String?
  roofSlopeDeg         Float?
  azimuthDeg           Float?
  tiltDeg              Float?
  purlinToPurlinM      Float?
  rafterToRafterM      Float?
  columnToColumnM      Float?
  skylightToSkylightM  Float?
  jackRoofDimM         String?
  ventilationDimM      String?
  lightningProtectionM Float?
  serviceLadderM       Float?
  otherNotes           String?
  roofAges             SurveyRoofAge[]
  survey               SiteSurvey      @relation(fields: [surveyId], references: [id], onDelete: Cascade)
}

model SurveyRoofAge {
  id          String          @id @default(cuid())
  structureId String
  roofType    String
  ageYear     Int
  structure   SurveyStructure @relation(fields: [structureId], references: [id], onDelete: Cascade)
}

model SurveyQA {
  id                           String     @id @default(cuid())
  surveyId                     String     @unique
  dayNightUsage                String?
  dayNightUsageDetail          String?
  dayLoad                      String?
  nightLoad                    String?
  backupLoad                   String?
  backupLoadDetail             String?
  batteryChargeMode            String?
  batteryChargeDetail          String?
  gridChargeAtNight            String?
  gridChargeAtNightDetail      String?
  inverterBackupOnOutage       String?
  inverterBackupOnOutageDetail String?
  powerQualityIssue            String?
  powerQualityIssueDetail      String?
  solarReasons                 String[]   @default([])
  solarReasonsDetail           String?
  survey                       SiteSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
}

model SurveyPhoto {
  id        String     @id @default(cuid())
  surveyId  String
  photoType String
  fileUrl   String
  photoDesc String?
  survey    SiteSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
}

model SurveyDocument {
  id               String     @id @default(cuid())
  surveyId         String
  documentType     String
  customerProvided Boolean    @default(false)
  fileUrl          String
  note             String?
  survey           SiteSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
}

model SurveyBill {
  id        String     @id @default(cuid())
  surveyId  String     @unique
  billCount Int?
  fileUrl   String
  note      String?
  survey    SiteSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model announcements {
  id         Int      @id @default(autoincrement())
  title      String   @db.VarChar(200)
  content    String?
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  created_by Int?
  admins     admins?  @relation(fields: [created_by], references: [id])
}

model FuelFlagReview {
  id         Int       @id @default(autoincrement())
  employeeId String    @db.VarChar(20)
  flagDate   DateTime  @db.Date
  flagType   String
  reviewedBy String?
  reviewedAt DateTime? @db.Timestamptz(6)
  note       String?
  createdAt  DateTime  @default(now()) @db.Timestamptz(6)

  @@unique([employeeId, flagDate, flagType])
}

model fleetcards {
  id      Int    @id @default(autoincrement())
  card_no String @unique
  emp_id  String @db.VarChar(20)
}

model fleetcard_transactions {
  id      Int      @id @default(autoincrement())
  card_no String
  date    DateTime @db.Timestamptz(6)
  liters  Decimal  @db.Decimal(10, 2)
  amount  Decimal  @db.Decimal(10, 2)
  status  String   @db.VarChar(50)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model company_settings {
  id         Int      @id @default(autoincrement())
  tax_id     String   @db.VarChar(13)
  name       String   @db.VarChar(200)
  address    String
  branch_no  String?  @db.VarChar(50)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tax_configs {
  id                  Int      @id @default(autoincrement())
  year                Int      @unique
  sso_rate            Decimal  @db.Decimal(5, 2)
  sso_max_monthly     Decimal  @db.Decimal(10, 2)
  sso_max_yearly      Decimal  @db.Decimal(10, 2)
  expense_deduct_rate Decimal  @db.Decimal(5, 2)
  expense_deduct_max  Decimal  @db.Decimal(10, 2)
  personal_allowance  Decimal  @db.Decimal(10, 2)
  tax_brackets        Json
  updated_at          DateTime @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model withholding_tax_documents {
  id                           Int                            @id @default(autoincrement())
  emp_id                       String                         @db.VarChar(20)
  book_number                  String?
  document_number              String                         @unique
  copy_type                    Int
  payer_name                   String
  payer_tax_id                 String                         @db.VarChar(13)
  payer_address                String
  payee_name                   String
  payee_tax_id                 String                         @db.VarChar(13)
  payee_address                String
  tax_form_type                String
  total_payment_amount         Decimal                        @db.Decimal(10, 2)
  total_tax_withheld           Decimal                        @db.Decimal(10, 2)
  total_tax_text               String
  deduct_provident_fund_1      Decimal?                       @db.Decimal(10, 2)
  deduct_social_security       Decimal?                       @db.Decimal(10, 2)
  deduct_provident_fund_2      Decimal?                       @db.Decimal(10, 2)
  payment_condition            Int
  payment_condition_other_desc String?
  signer_name                  String
  issue_date                   DateTime                       @db.Date
  created_at                   DateTime                       @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime                       @default(now()) @db.Timestamptz(6)
  employees                    employees                      @relation(fields: [emp_id], references: [emp_id])
  withholding_tax_income_items withholding_tax_income_items[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model withholding_tax_income_items {
  id                        Int                       @id @default(autoincrement())
  document_id               Int
  income_type_id            Int
  income_description        String?
  payment_date              DateTime                  @db.Date
  payment_amount            Decimal                   @db.Decimal(10, 2)
  tax_withheld              Decimal                   @db.Decimal(10, 2)
  withholding_tax_documents withholding_tax_documents @relation(fields: [document_id], references: [id], onDelete: Cascade)
}

model ServiceSchedule {
  id             String   @id @default(cuid())
  userId         String
  date           DateTime @db.Date
  status         String
  jobType        String?
  jobDescription String?
  duration       String?
  province       String?
  createdAt      DateTime @default(now()) @db.Timestamptz(6)
  updatedAt      DateTime @default(now()) @updatedAt @db.Timestamptz(6)
  user           User     @relation(fields: [userId], references: [id])
}

model PushSubscription {
  id        String   @id @default(cuid())
  userId    String
  endpoint  String   @unique
  p256dh    String
  auth      String
  userAgent String?
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  user      User     @relation(fields: [userId], references: [id])
}
